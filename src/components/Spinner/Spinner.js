import React from 'react';
import { SpinnerOverlay, SpinnerContainer } from './styled';

const Spinner = () => (
  <SpinnerOverlay>
    <SpinnerContainer />
  </SpinnerOverlay>
);

export default Spinner;
